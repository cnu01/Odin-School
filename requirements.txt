# Latest FastAPI Stack - August 2025
fastapi[standard]==0.116.1
motor==3.7.1          # includes pymongo
pydantic==2.9.2
uvicorn[standard]==0.32.0
python-dotenv==1.0.1
python-multipart==0.0.20

# Amazon Bedrock integration for TrustDesk
boto3==1.35.63        # AWS SDK for Python (Bedrock support)

# RAG and Vector Operations
numpy>=1.26.0         # Vector similarity calculations (Python 3.13 compatible)

# AWS Vector Database Options
opensearch-py==2.3.1  # Amazon OpenSearch client
aws-requests-auth==0.4.3  # AWS auth for OpenSearch
requests-aws4auth==1.2.3  # Alternative AWS auth

# Document Processing Dependencies for RAG
PyMuPDF==1.23.22      # PDF processing (fitz)
python-docx==1.1.0    # Word document processing
python-magic==0.4.27  # File type detection
python-magic-bin==0.4.14  # Windows binary for python-magic

# Optional but useful (testing, http client)
httpx==0.27.2
pytest==8.3.3
pytest-asyncio==0.24.0

# Legacy AI dependencies (kept for compatibility)
openai>=1.0.0,<2.0.0
